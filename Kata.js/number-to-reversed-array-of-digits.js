const digitize = (n) => {
  const string = n.toString()
  const array = string.split('')
  const reverse = array.reverse('')
  return reverse.map((digit) => {
    return parseInt(digit)
    let n = []
  })

  return n.toString().split('').reverse().map(digit => parseInt(digit))
}
